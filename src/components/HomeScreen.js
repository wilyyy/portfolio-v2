import styled from "styled-components";

const Home = styled.div`
   display: flex;
   flex-direction: column;
`;

export default function HomeScreen() {
   return (
      <Home>
         The practice of William Laurel Alvarez, a web / mobile developer based in
         Vancouver, Canada
      </Home>
   );
}
